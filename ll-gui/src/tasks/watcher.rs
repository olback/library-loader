// use library_loader_core::{Watcher, LLResult, TX, NotifyError};

// pub struct WatcherHandler {
//     channel: TX,
//     join_handle: std::thread::JoinHandle<LLResult<()>>
// }

// impl WatcherHandler {

//     pub fn stop(&self) {
//         self.channel.send(Err(NotifyError::generic("stop"))).unwrap();
//     }

// }

pub fn watcher() /* -> LLResult<WatcherHandler> */ {

    // let jh = std::thread::spawn(move || {
    //     w.start()?
    // });

    // Ok(WatcherHandler {
    //     channel: c,
    //     join_handle: jh
    // })

}
