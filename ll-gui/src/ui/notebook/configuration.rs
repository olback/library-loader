use crate::types::AMState;
use gtk::{Builder};

#[derive(Debug, Clone)]
pub struct Configuration {

}

impl Configuration {

    pub fn build(builder: &Builder, state: &AMState) -> Self {

        Self {

        }

    }

}
